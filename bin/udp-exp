#!/bin/bash
while read user exp max; do
  today=$(date +%s)
  exptime=$(date -d "$exp" +%s)
  if [[ $today -ge $exptime ]]; then
    sed -i "/^$user/d" /etc/udp-zivpn/users.db
    python3 /etc/udp-zivpn/bot_notify.py "$user expired"
  fi
done < /etc/udp-zivpn/users.db
